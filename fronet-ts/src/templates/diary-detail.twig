<div class="container mx-auto py-8">
  <h1 class="text-3xl font-bold mb-4 text-brown-900">{{ diary.diaryTitle }}</h1>
  <p class="text-brown-500">{{ diary.diaryContent }}</p>
  <div class="mt-4">
    <p class="text-brown-700">{{ diary.diaryTime }}</p>
  </div>
  <div class="mt-8">
    <h2 class="text-xl font-semibold mb-2 text-brown-800">Emotions</h2>
    <!-- Display emotion bar here -->
  </div>
  <div class="mt-8">
    <!-- Display alerts and recommendations here -->
  </div>
</div>
